1. VCO ring since it doesn't have a startup circuitry here, needs some settings in the virtuoso tool before simulation to help it converge(gives vout an initial value either 0/1). Additionally also need to give it min current and voltage it can detect, to be able to run. otherwise no simulation seen.
2. the caps are only used to add delay. the output one one stage will charge and then discharge the cap before being the input of the second stage. thus time delay added.
3. IMP: lets say when pull up ckt is ON. the header pmos acting as active load(it is on thus rest is very low, but some voltage drop across it say Vx). Thus voltage available at source of pmos of the inverter is Vdd-Vx. this pmos is also ON when pull up is ON, and drain of pmos ie out noe rises to Vdd-Vx.
however when pull up is off, there is some leakage from header and output voltage keeps rising, and goes close to Vdd.
Then why does this voltage that is lesser than Vdd at the output of first inv, after propogating thr the entire chain not become very less than Vdd? because at the second inv the output will again become 0!!!! and this 0 will get an all new chance at the 3rd(odd) inverter to become Vdd-Vx. so it doesnt keep reducing by Vx at each inv stage.
4. Why the inverter is pplaced at the output of the VCO? not to invert the output of the VCO to match the input freq. even if there is phase delay between input and the feedback provided by the vco, that is the job of the LDO to automatically rise and fall the freq of the feedback signal until the phase and freq match. So we don't need to invert signal from ldo to go and match input signal
then why?? to address (2). To finally bring the output again back to rail ie vdd and vss
5. we need to design vco such that it produces the desired freq at vcontrol= supply/2.